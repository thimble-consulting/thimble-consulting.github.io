import React from "react";
import ContactUsForm from "../ContactUsForm";

const Modals = ({ children, className = "", size, onClick }) => {
  return (
    <>
      <ContactUsForm />
    </>
  );
};

export default Modals;
